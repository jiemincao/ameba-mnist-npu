# 怎麼把 `sensor_f37.bin` 解出來

> 這份是**方法**的教材，不是結果報告。
> 結果在 [docs/f37_regs.txt](docs/f37_regs.txt)，工具是 [scripts/decode_sensor_bin.py](scripts/decode_sensor_bin.py)。
>
> 架構背景 → [SDK_ARCH.md](SDK_ARCH.md)

---

## 0. 先更正我上次講錯的一句話

我之前說：「F37 的設定根本不是 C 程式碼，是資料 blob。」

**只對一半。** 正確的是：

> `sensor_f37.bin` 是一個**編譯好的 sensor driver 模組**，
> 裡面同時有 **ARM Thumb-2 機器碼**和一張**暫存器初始化資料表**。
> 我們要的那張表是它的 `.data` 區。

為什麼這個更正重要：如果你以為整個檔案都是資料，你會想「從頭 parse 到尾」——
那會在讀到程式碼時解出一堆垃圾。知道它是「資料在前、程式碼在後」，
才會去找**兩者的交界**在哪裡，而那正是整個解碼的關鍵。

---

## 1. 這個檔案是誰在用的

```
KM4                                    VOE 協處理器
─────────────────────────              ──────────────────────
video_init(iq_addr, sensor_addr)
  hal_video_load_sensor(...)  ──搬──▶  載入到 0x70000000
                                        執行裡面的 Thumb 程式碼
                                        程式碼讀自己的資料表
                                        逐筆用 I2C 寫進 F37 晶片
```

所以它不是跑在你熟的 Cortex-M33（KM4）上，**是跑在 VOE 那顆 CPU 上**。
這就是為什麼 SDK 裡找不到 `sensor_f37.c`——它不屬於 KM4 的編譯樹。

---

## 2. 解碼的推理過程（這段才是重點）

### 步驟 1：先看檔頭，找「自我驗證」的欄位

```
00000000: ea07 061d 5254 4c38 3733 3542 5f56 4f45  ....RTL8735B_VOE
00000010: 5f31 2e37 2e31 2e30 0000 0000 6015 0000  _1.7.1.0....`...
00000020: f906 0070 5d02 0070 0000 0000 0000 0000  ...p]..p........
```

拆開來：

| 位移 | 大小 | 內容 |
|---|---|---|
| 0x00 | 4 | magic `EA 07 06 1D` |
| 0x04 | 20 | 版本字串 `RTL8735B_VOE_1.7.1.0` |
| 0x18 | 4 | 0 |
| **0x1C** | **4** | **`0x00001560` = 5472** |
| 0x20 | 4 | `0x700006F9` |
| 0x24 | 4 | `0x7000025D` |

**關鍵動作：驗證 0x1C 那個數字。**

```
5472 + 32 (檔頭) = 5504 = 檔案大小 ✓
```

對上了 → 它是 payload 長度，**而且證明我對檔頭長度 0x20 的猜測是對的**。

> **這是逆向的第一原則：先找一個能自我驗證的欄位。**
> 一個「加起來剛好等於檔案大小」的數字，比任何猜測都可靠。
> 而且我拿 gc4653（3880）、imx307（12856）再驗一次，三顆全中——不是巧合。

0x20 / 0x24 那兩個 `0x7000xxxx` 我**沒有證實**用途（形式像函式指標，
但指到的位置不在指令邊界上）。不確定的就寫不確定，不要腦補。

### 步驟 2：發現資料表

0x4C 開始出現極規律的模式：

```
1200 4000 | 4800 8a00 | 4800 0a00 | 0e00 1900 | 0f00 0400
 ^^   ^^     ^^   ^^
 低位元組有值，高位元組是 00
```

拆成 4 byte 一組、每組兩個 little-endian u16：

```
(0x0012, 0x0040)   →  reg 0x12 = 0x40
(0x0048, 0x008A)   →  reg 0x48 = 0x8A
(0x000E, 0x0019)   →  reg 0x0E = 0x19
```

查 JXF37 的公開資料：**reg 0x12 = 0x40 是 standby**。
sensor 初始化的標準開頭就是先進 standby。**方向對了。**

### 步驟 3：⭐ 找到表的結尾——用「兩個獨立方法互相驗證」

這是整件事最容易出錯的地方。我用了兩個**完全不相干**的判斷，看它們會不會指向同一個位置。

**方法 A（看資料）**：值必須是 8-bit。一旦某筆的「值」欄位高位元組不是 0，就不是表了。

**方法 B（看程式碼）**：往後掃，找第一個像 Thumb 函式開頭的 16-bit 指令
（`push {..., lr}` 或 `ldr rX, [pc, #imm]`）。

結果：

| sensor | 方法 A 說表結束於 | 方法 B 說程式碼開始於 |
|---|---|---|
| f37 | 0x23C | **0x23C** |
| f35 | 0x368 | **0x368** |
| gc4653 | 0x28C | **0x28C** |
| imx307 | 0x228 | **0x228** |
| sc2336 | 0x2A4 | **0x2A4** |

**五顆全部完全重合。** 兩個毫無關聯的判準給出同一個答案，
這比任何單一方法「看起來合理」都有說服力。

> 如果只用方法 A，你永遠不知道自己是不是多吃了幾筆垃圾。
> **逆向要的不是「解得出來」，是「知道自己解對了」。**

### 步驟 4：反組譯確認那真的是程式碼

```bash
arm-none-eabi-objdump -D -b binary -m armv7e-m -M force-thumb \
    --adjust-vma=0x70000000 --start-address=0x7000023C sensor_f37.bin
```

```asm
7000024c:  b510        push  {r4, lr}
7000024e:  2800        cmp   r0, #0
70000250:  d142        bne.n 0x700002d8
70000256:  f44f 62f0   mov.w r2, #1920   ; 0x780     ★
7000025a:  f44f 6387   mov.w r3, #1080   ; 0x438     ★
7000027a:  e9c1 2302   strd  r2, r3, [r1, #8]
```

**1920 和 1080 直接出現在指令的立即數裡**，然後一起寫進某個結構的 +8 / +12 欄位。
那是 F37 的輸出解析度。到這裡已經沒有懷疑空間了。

順帶：`ldr r3,[pc,#4]` 的常數池指向 `0x70000048`，而檔案位移 0x48 剛好是
資料表前面 4 個 byte 的零 —— 一個初始值為 0 的變數。
**這同時證明了載入基底就是 `0x70000000`，檔案位移 = 位址低位。**

### 步驟 5：拿別顆 sensor 打自己的臉

我原本的解析器寫死「位址必須 ≤ 0xFF」。拿 gc4653 和 imx307 一跑 → **0 筆**。

去看原始 byte：

```
imx307:  0530 0100 | 0730 0000 | 0930 0100 | 0a30 f000
         → reg 0x3005 = 0x01,  reg 0x3007 = 0x00,  reg 0x3009 = 0x01
```

`0x3005 / 0x3007 / 0x3009` —— 這是 **Sony IMX307 的標準暫存器位址**。
格式完全一樣，**只是位址寬度不同**：F37/F35 用 8-bit，IMX307/GC4653 用 16-bit。

> **只拿一個樣本驗證，等於沒驗證。**
> 我的假設在 F37 上「完美成立」，但它是錯的——只是剛好被 F37 的特性掩蓋了。

---

## 3. 解出來的結果

### F37（AMB82-MINI 上那顆）

```
暫存器表 : 0x4C .. 0x23C   共 124 筆   位址寬度 8-bit
程式碼   : 0x23C 起 (載入後 0x7000023C)

VTS (每幀行數) = 0x0465 = 1125
HTS (每行長度) = 0x0500 = 1280
reg 0x12 首次寫入 = 0x40 (standby)，最後一次 = 0x00 (開始串流)
```

**VTS = 1125** 是 1080p 的標準幀長（1080 有效行 + 45 行 blanking）。
這個數字不是我算出來的，是解出來的——**它自己對上了規格，所以解碼是對的。**

頭尾也對：第 0 筆 `reg 0x12 = 0x40` 進 standby，第 119 筆 `reg 0x12 = 0x00` 開串流。
這正是每一份 sensor init sequence 的標準結構。

⚠️ **已知不確定**：第 120~123 筆（`0x00=0x10`、`0x40=0x00`、`0x40=0x00`、`0x46=0x00`）
出現在「開串流」之後，位置可疑。它們也可能不是暫存器對，而是模組的 4 個 u32 資料欄位。
沒證實之前就標記成不確定，不要當成事實用。

完整 124 筆 → [docs/f37_regs.txt](docs/f37_regs.txt) / [docs/f37_regs.csv](docs/f37_regs.csv)

### 全部 85 顆

```
8-bit 位址: 49 顆    16-bit 位址: 36 顆
```

清單 → [docs/sensor_bin_index.txt](docs/sensor_bin_index.txt)

---

## 4. 工具怎麼用

```bash
# 看 F37
python scripts/decode_sensor_bin.py <voe_bin>/sensor_f37.bin

# 順便存成 CSV
python scripts/decode_sensor_bin.py <voe_bin>/sensor_f37.bin --csv docs/f37_regs.csv

# 任何一顆都可以（位址寬度自動判斷）
python scripts/decode_sensor_bin.py <voe_bin>/sensor_imx307.bin
```

`<voe_bin>` =
`ameba-rtos-pro2/component/soc/8735b/fwlib/rtl8735b/lib/source/ram/video/voe_bin/`

---

## 5. 這張表能拿來做什麼

| 想做的事 | 用得上嗎 | 說明 |
|---|---|---|
| **看懂相機怎麼被設定起來** | ✅ | 124 筆從 standby 到串流的完整順序 |
| 確認目前的解析度 / 幀率設定 | ✅ | VTS 1125、HTS 1280 直接讀出來 |
| 對照 datasheet 學 sensor | ✅ | 有實際生效的數值可以對 |
| **執行期改曝光 / 增益** | ⚠️ 不用改這裡 | 走 `isp_ctrl_api.h` 的 API，見 [SDK_ARCH.md](SDK_ARCH.md) §4.2 |
| 改 bin 換解析度 | ❌ **不要** | 有 checksum 風險，而且 VOE 韌體對格式的假設我們沒全部搞懂 |

> 這張表是**讀的**，不是拿來改的。要動參數走 VOE 的 ISP 指令介面。

---

## 6. 可以帶走的三條通則

1. **先找能自我驗證的欄位**（長度 = 檔案大小）。有一個錨點，後面才站得住。
2. **兩個獨立方法指向同一個答案，才算解對。** 這裡是「資料的規律」對上「程式碼的起點」。
3. **只有一個樣本等於沒驗證。** 我的第一版在 F37 上 100% 正確，但格式假設是錯的。
